import React from "react";
import * as s from "./Main.module.scss"
type PropsType = {

};
// Main
const Main: React.FC<PropsType> = React.memo((p) => {

	// Return
	return (
		<div>

		</div>
	)
});

export default Main